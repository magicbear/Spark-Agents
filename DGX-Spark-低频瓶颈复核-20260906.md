# DGX Spark 四机低频瓶颈复核

> 本文为历史阶段记录。最新实测结果、运行配置与服务状态请以[逐项优化实测报告](DGX-Spark-逐项优化实测报告-20260906.md)为准。


日期：2026-09-06。承接 opencode 会话 `ses_f8de42029ffecbKh7tMukpcphf` 与《DGX-Spark-四机优化记录-20260906.md》。

> 后续配置变更：用户要求将上下文调至1M，现已完成，详见 [1M上下文配置记录](DGX-Spark-1M上下文配置-20260906.md)。本文性能A/B使用变更前32K配置。

> 最新结果：完成 `.244/.246/.250` 正常关机和现场完整断电后，三台本地 BF16 算力恢复至约 77–81 TFLOPS。已按原参数恢复四机服务。首轮同口径测得单请求生成约 49–52 tok/s、六请求真实墙钟吞吐 31.96 tok/s，分别比故障基线提高约 70% 和 102%。详细恢复结果见文末；文中较早的“等待断电/服务停止”段落是过程记录。

## 结论与证据强度

**已确认三台节点存在真实的 GPU 低频计算性能异常。** 不能再把四机当前表现当作正常硬件状态下的推理性能上限，也不能把频率差异直接解释为遥测不可信。

在 vLLM 空闲时，四台分别执行相同、完全不使用网络的 BF16 4096×4096 矩阵乘法。每台预热 10 次，用 CUDA Event 测量每组 20 次矩阵乘法，持续约 8 秒，取各组平均单次时间的中位数。矩阵 A/B/C 合计约 96 MiB；另有 PyTorch/CUDA 上下文开销。原服务保持加载，测试前各节点可用内存约 23–25 GiB。

| 节点 | BIOS | 单次 GEMM 中位数 | 实测 BF16 TFLOPS | 计算期间 SM 频率 | 采样峰值 GPU 功耗 |
|---|---|---:|---:|---|---:|
| .246 | 0104 | 4.74 ms | 29.02 | 637–643 MHz | 16.83 W |
| .250 | 0104 | 4.76 ms | 28.86 | 约 637 MHz | 16.80 W |
| .248 | 0105 | 1.52 ms | 90.41 | 约 2242–2281 MHz | 92.87 W |
| .244 | 0104 | 5.94 ms | 23.12 | 507–513 MHz | 15.35 W |

四台驱动均为 580.142，CUDA 13.0；同一测试中 GPU 利用率均可达约 96%。正常节点的实际矩阵计算速度是异常节点的 **3.1–3.9 倍**。这证明差异不仅存在于频率传感器读数中。

这是计算微基准的比值，**不是推理加速承诺**。LLM 包含权重读取、稀疏算子、调度和多机通信，恢复后实际 token/s 增幅必须复测。

## 当前瓶颈解释

1. GPU-Util 表示采样窗口内 GPU 执行 kernel 的时间占比，不等于峰值 FLOPS 使用率；低频运行、低占用 kernel、通信等待也可以产生高利用率。
2. TP/EP 分布式执行有同步依赖。慢节点延迟会拖慢整个组，快节点等待时既不需要高功率，也不能持续产生满带宽流量。
3. 100Gbps 大包链路测试正常，不代表每层的小消息 collective 延迟低；低平均带宽不能排除通信等待。不过此次纯本地计算已先确认明显硬件状态问题，应该优先恢复再研究 collective。
4. `nvidia-smi power.draw` 与整机墙上功耗不是一个口径。但本次相同工具、相同计算负载测到 15–17W 对 93W，结合计算速度差异，不能简单用“测量口径”排除异常。

## 软件恢复尝试

对最慢的 `.244` 执行了 `nvidia-smi -rgc`，命令返回 `All done.`；随后再次运行相同微基准：**23.15 TFLOPS，507–513MHz，峰值约 15.28W**。恢复默认 GPU 时钟策略没有解除异常。

没有修改 vLLM 启动脚本、网络、驱动或 BIOS，没有重启服务或主机。测试结束 `/health` 返回 200，running/waiting 均为 0。

## 尚待验证的底层原因

- **电源协商/固件状态异常是候选原因，不是已证实根因。** NVIDIA 官方论坛有低频、高 GPU 利用率、低功耗、无活动降频告警的同类案例，部分通过完全断电恢复；也有未恢复的报告。
- BIOS 版本与健康状态完全对应：异常三台为 `GX10DGX.0104.2026.0326.1657`，正常 `.248` 为 `GX10DGX.0105.2026.0505.1153`。这是相关性，未进行 BIOS A/B，不能据此保证升级有效。
- `.246` 开机日志有 GPU PCIe DOE mailbox reset/create 失败。其刚于约两小时前重启，低频仍存在，说明此前恢复网卡带宽的软重启并未同时恢复 GPU 状态。另三台当前 dmesg 搜索未返回对应信息，不能据此断言它们开机从未报错。
- 快照中的 SW/HW thermal/power throttle reason 均不活动。这不推翻独立计算实测的低频异常；历史累计 Power Capping 时间也不能当作当前限功率证据。
- `GPU T.Limit Temp` 是距离热限制的余量，不是“温控阈值被锁到 50°C”。`mlx5 insufficient power (27W)` 是网卡日志，不能直接解释为 GPU 27W 功率上限。

## 恢复验证步骤

下一步需要现场或已授权的 PDU 操作，尚未执行：

1. 安排四机推理维护窗口，正常停止四机服务。优先以最慢的 `.244` 做单节点恢复实验，避免同时更改所有变量。
2. 正常关机后，完全断开该机器与电源适配器的供电，包括适配器交流输入；移除可能反向供电的连接，等待数分钟后重新连接原装电源开机。避免在机器运行或文件系统仍在写入时直接断电。
3. 先不加载模型，运行保留的 `compute_probe.py`，与恢复前 23.12 TFLOPS 对比；目标是负载下接近正常节点约 2.2GHz、90 TFLOPS 的量级，具体随热状态变化。
4. 若明显恢复，再按相同步骤处理 `.246/.250`。若未恢复，继续核对原装电源及连接、ASUS BIOS/EC/PD 固件版本与官方更新说明；升级固件须另行安排，不能盲刷。
5. 节点恢复后验证 MTU 9000、BeeGFS 路由 MTU 2044、挂载读取；先起三个 worker，再起 head，确认健康状态。
6. 用同一份 `benchmark.py` 重测，计算真实墙钟总吞吐。确认硬件恢复与业务改善之间的实际关系。

仅恢复 `.244` 后，其余两个低频节点仍可能限制四机吞吐，因此单节点实验应以本地计算恢复作为首要判据。

## 本次推理基线

保持 TP=4、EP 开启、seqs=1、batched_tokens=1024、DSpark=5。固定英文提示文本、temperature=0、seed=42、ignore_eos=true，直接从 head 本地访问 API，保存完整 usage、流式 chunk 时间和 Prometheus 指标差值。

| 输入 tokens/请求 | 输出 tokens/请求 | 提交并发 | TTFT | 生成阶段耗时 | 真实墙钟总输出吞吐 |
|---:|---:|---:|---|---|---:|
| 118 | 512 | 1 | 0.317 s | 17.917 s | 28.06 tok/s |
| 3841 | 512 | 1 | 4.195 s | 16.579 s | 24.64 tok/s |
| 3841 | 128 | 6 | 4.48–44.46 s | 3.21–4.14 s/请求 | 15.80 tok/s |

六条请求由 seqs=1 串行执行。最后一列是总输出 token 除以整个批次墙钟时间，包含排队与 prefill。流式 chunk 可能含多个 token，不能把 chunk 数或 chunk 间隔直接当作 token 数/TPOT。

主节点同步 80 个采样点内 GPU 利用率峰值 96%，SM 始终 643MHz，GPU 功耗最高 11.58W。该测试窗口内主节点所读取的 RDMA 错误/重传计数没有增加；这不是对所有节点、所有负载的无丢包证明。

DSpark 指标差值：

| 测试 | 草稿轮数 | 草稿 token | 接受 token | 接受率 | 1+接受/轮 |
|---|---:|---:|---:|---:|---:|
| 短输入、512输出 | 211 | 1055 | 304 | 28.8% | 2.44 |
| 长输入、512输出 | 194 | 970 | 320 | 33.0% | 2.65 |
| 长输入、六请求 | 260 | 1300 | 505 | 38.8% | 2.94 |

最后一列是投机统计口径，终止截断等因素使其不必严格等于请求完成 token/轮数。短输入测试第5个草稿位置只接受6次/211轮，提示减少草稿长度值得在硬件恢复后 A/B；仅凭接受率不能判断投机是否整体加速。

## 硬件恢复后的优化顺序

1. **真实 kernel 时间分析**：启用有限迭代的 Torch profiler 或 Nsight，分别看 prefill/decode 的 NCCL、MoE、attention、CPU 调度及 rank 等待。当前服务没有启用 profiler，未采集 kernel trace，不能报告通信占比。
2. **DSpark 5 对关闭**：每次只改一个因素，固定请求，比较端到端、生成速度与接受率。后续已核实当前源码明确禁止草稿数低于 checkpoint 的 dspark_block_size=5，存在输出正确性问题；撤回早期 5 对 3 的候选建议，不应绕过校验。详见 [后续优化调研](DGX-Spark-后续优化调研-20260906.md)。
3. **TP=2 对 TP=4、EP 开关**：两机减少同步参与者，可能改善单请求延迟，但每节点权重/缓存占用上升，先核算内存。两组双机副本还可能提高总吞吐；均属待测方案。
4. **并发和 prefill 预算**：先固定 seqs 测 1024/2048/4096，再固定预算测 seqs 1/2/4/8，同时比较 TTFT、生成时间与真实总吞吐。1024 主要可能限制 prefill，不能从旧 A/B 直接推导“六请求 decode 被1024卡住”。
5. **通信延迟**：在空闲/维护条件下测与真实激活尺寸相近的小消息 AllReduce/AllGather/ReduceScatter，而非只跑64MiB吞吐。当前 EP 默认 all2all backend 为 allgather_reducescatter，TP 使用 PYNCCL。
6. **重复前缀缓存、算子融合与双 rail**：分别针对已测出的瓶颈评估。双 rail 只有在通信数据传输带宽确为限制、且路由隔离正确时才值得优先部署。

## 原始证据与复现

目录：`/data/clawdata/dgx-analysis-20260906/`

- `compute_probe.py`：独立本地矩阵计算测试。
- `compute-{246,250,248,244}.json`：四机原始计算耗时和GPU采样。
- `compute-244-after-reset.json`：恢复默认时钟后的复测。
- `benchmark.py`、`baseline.jsonl`：推理基线脚本及完整结果/指标/采样。
- `hardware-{246,250,248,244}.txt`：BIOS、运行时长、相关日志及降频事件快照。

从工作目录执行，例如：

```bash
ssh -F /dev/null root@192.168.32.244 '/opt/runtimes/vllm/bin/python -' < dgx-analysis-20260906/compute_probe.py
ssh -F /dev/null root@192.168.32.246 'python3 -' < dgx-analysis-20260906/benchmark.py
```

仅在没有其他推理请求时跑独立计算测试，避免污染基线；脚本不自动停服务或关机。

## 现场断电实验进度

用户随后明确确认“可以关机”。已核验请求 running/waiting 均为 0，停止四机 vLLM 服务，对未及时退出的 worker 使用 SIGKILL 清理；确认 `.244` 无 vLLM 进程、无 GPU 计算进程后，执行 `sync; systemctl poweroff`，命令成功返回。等待现场对 `.244`（`gx10-7c63`）及电源适配器完全断电后再开机复测。其余三台没有执行系统关机。此阶段四机推理服务处于停止状态，不再是前文记录的服务在线状态。

## .244 完整断电后复测

用户完成现场断电并开机后，`.244` 运行相同 `compute_probe.py`：**77.32 BF16 TFLOPS**，负载稳定频率约 **2346MHz**，采样峰值 **90.62W**。恢复前为 23.12 TFLOPS、507–513MHz、15.35W，计算性能提升约 **3.34 倍**。原始结果为 `dgx-analysis-20260906/compute-244-after-powercycle.json`。未升级 BIOS 或驱动。这支持可通过完整断电解除的低频状态异常，但尚不能确定具体是电源适配器、协商还是固件内部状态。

重启后四个数据接口 MTU 9000、RoCE active_mtu 4096、BeeGFS 路由 MTU 2044 均保留。GPU DOE mailbox 错误仍出现在本次启动日志中，但计算性能已经恢复，因此不能将该日志单独作为当前 GPU 低频的判据。BeeGFS 的 `mnt-beegfs.mount` 开机失败，日志为无法解析管理主机地址 `192.168.32.2`；手动执行 `systemctl start mnt-beegfs.mount` 后已恢复挂载，模型 config.json 可读取。开机失败的具体原因尚未确认，未更改持久化挂载配置。其余节点和四机推理暂未重启。

## .246/.250 断电恢复进度

在 `.244` 断电后恢复性能并修复挂载后，用户确认准备好操作另外两台。已确认 `.246`（gx10-a14b）和 `.250`（gx10-723a）没有 vLLM 进程，分别执行 `sync; systemctl poweroff`，命令均成功返回。等待现场完整断电并重新开机；`.244/.248` 保持开机，四机推理服务仍停止。

## 三台恢复与四机业务复测

用户完成 `.246/.250` 现场断电开机后，四台在模型未加载条件下重新运行同一计算探针：

| 节点 | 故障期 BF16 TFLOPS | 恢复后 BF16 TFLOPS |
|---|---:|---:|
| .246 | 29.02 | 78.89 |
| .250 | 28.86 | 81.15 |
| .248（未断电） | 90.41 | 90.99 |
| .244 | 23.12 | 77.02 |

`.250` 首次计算探针里频率读数仍短暂显示208MHz，但81.15 TFLOPS与约93W实际功耗已恢复。之后真实推理时四台SM频率分别读到2405/2411/2483/2398MHz。这说明**不能单独信任一个频率快照**；之前用实际矩阵计算时间确认性能异常的方法是必要的。

`.246/.250` 开机也出现与 `.244` 相同的 BeeGFS 挂载失败；通过 `systemctl start mnt-beegfs.mount` 恢复，模型配置可读取。四台接口MTU均9000、RoCE active_mtu均4096、到BeeGFS路由MTU均2044。开机挂载时序问题未做持久化修复，需要后续独立处理。

按原脚本先启动`.250/.248/.244`，最后启动`.246`；未改变模型、TP=4、EP、seqs=1、batched_tokens=1024、DSpark=5等参数。日志：各节点 `/var/log/ds4f-t4-<节点末段>-powercycle.log`。约04:13服务健康接口恢复200，完成相同基准。

| 指标 | 故障基线 | 恢复后首轮 | 变化 |
|---|---:|---:|---:|
| 118输入/512输出，生成阶段约tok/s | 28.58 | 49.21 | +72.2% |
| 3841输入/512输出，生成阶段约tok/s | 30.88 | 52.48 | +69.9% |
| 3841输入/512输出，TTFT | 4.195s | 2.286s | -45.5% |
| 六请求各3841输入/128输出，墙钟吞吐 | 15.80 tok/s | 31.96 tok/s | +102.3% |
| 六请求批次墙钟耗时 | 48.62s | 24.03s | -50.6% |

生成阶段使用输出usage/首末内容chunk间隔，为近似口径；完整墙钟吞吐按批次计时。六请求仍由seqs=1串行处理，不代表同时运行六条请求。短输入首轮TTFT从0.317s到0.435s，属于服务刚启动后的首请求，不能宣称所有场景TTFT均改善。

恢复后首轮主节点推理采样功耗峰值47.2W，频率最高2405MHz；读取的RDMA错误/重传计数没有增加。实际decode时刻各节点功耗约24–34W，仍明显低于纯GEMM的90W。这不否定性能已恢复：decode的算子形状、内存访问及跨机同步与纯GEMM不同，不能以“必须达到90W/跑满100Gbps”作为推理性能正确性的判据。通信、算子和调度的剩余占比仍需profile确认。

结果文件：`compute-*-restored.json`、`restored.jsonl`、`comparison.json`。`summarize.py`可重新生成前后比较。重复验证保存为`restored-repeat.jsonl`。

第二轮相同测试：短输入生成49.41 tok/s、TTFT 0.217s；长输入生成50.18 tok/s、TTFT 1.835s；六请求真实墙钟吞吐32.54 tok/s。两轮恢复后结果一致，改善不是一次偶然的页面读数。当前仅完成短时恢复验证，尚不能保证长时间运行、OOM或重启后不复发。

## 参考来源

- NVIDIA 对 GPU utilization 的定义：[NVIDIA-SMI 文档](https://docs.nvidia.com/deploy/nvidia-smi/index.html)。
- 同类实测低频与完整断电后恢复的第一手报告：[GPU pinned at 721MHz](https://forums.developer.nvidia.com/t/dgx-spark-gb10-gpu-clock-pinned-at-721-mhz-under-full-load-no-throttling-not-liftable-via-nvidia-smi/376039)。这是故障线索，不能替代本机恢复实验。
- 类似故障讨论、T.Limit 温度余量说明：[15W/650MHz case](https://forums.developer.nvidia.com/t/dgx-spark-grace-blackwell-gb10-performance-drop-gpu-trapped-in-15w-650mhz-loop-with-50-c-artificial-t-limit-temp/370304)。
- ASUS 官方固件更新流程：[GX10 操作系统和固件更新](https://www.asus.com.cn/support/faq/1056213/)。尚未下载或刷写固件。
